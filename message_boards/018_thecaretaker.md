I wanna build a app that runs on my mac mini that allows for automation that:

- trigger: cron job, trigger by new issue, etc
- action: trigger antigravity cli to handle that, trigger codex/claude code to review the work, and antigravity to execute the work, then codex/claude code to test the entire app out to confirm everything


all utilize the cli coding agents.

note to self: check out poteto skills (the grokbot engineering gal)